import React from "react";
import {
  LOGIN_REQUEST,
  LOGIN_SUCCESS,
  LOGIN_FAILURE,
  LOGOUT_REQUEST,
  LOGOUT_SUCCESS,
  LOGOUT_FAILURE,
} from "../actions/Auth";

const Auth = (action) => {
//   switch (action.type) {
//     case LOGIN_REQUEST: {
//       console.log("suc");
//     }
//   }
};

export default Auth;
